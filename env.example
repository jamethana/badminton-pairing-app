# Supabase Configuration
# Copy this file to .env.local and fill in your actual Supabase credentials

# Your Supabase project URL (found in your Supabase dashboard under Settings > API)
REACT_APP_SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase anon/public key (found in your Supabase dashboard under Settings > API)
REACT_APP_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Supabase CA Certificate (for SSL certificate validation)
# This can be the certificate content in PEM format or base64 encoded
# If not set, the app will try to load from public/prod-ca-2021.crt
# REACT_APP_SUPABASE_CERT=-----BEGIN CERTIFICATE-----...-----END CERTIFICATE-----

# For Node.js development (alternative to REACT_APP_SUPABASE_CERT)
# NODE_EXTRA_CA_CERTS=./public/prod-ca-2021.crt

# Note: If SUPABASE_URL and SUPABASE_ANON_KEY are not set, the app will run in offline mode using localStorage only
# For production deployment, make sure to set these environment variables
